@init
/*
a < 2 ? (
    a = 0;
    b = 0;
    c = 2;
);

b < 3 ? : (
    d = 0;
    e = 0;
    f = 0.2;
);

// testing simple lambda
x = g < 4 ? (
    h = 0;
    i = 0;
) : ( 3 );
*/
// testing lambda propagation
x = j < 3 ? (k < 5 ? 4 : 5) : 6;
