@init
function f2 (x*) (
    x.y;
);

function f (x*) local (n) (
    x.y = 3;
    x.z = 4;
    f2(x);
    n = pow (f2(x), 4.);
    n = pow (f2(paco), 4.);
);

function f3 (a, x*) (
    x.y = a;
);

f(paco);
f2(paco);
f3(1, paco);
